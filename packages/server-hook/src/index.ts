/**
 * Server Hook Package
 *
 * Provides server-side MCP initialization handling and lifecycle callbacks.
 * This hook monitors the MCP initialization process and provides callbacks
 * when initialization completes.
 */

export type { ServerHookConfig, ServerHookOptions } from "./hook.js";
export { ServerHook } from "./hook.js";
